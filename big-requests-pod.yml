apiVersion: v1
kind: Pod
metadata:
  name: big-requests-pod
spec: 
  containers:
    - name: big-container
      image: nginx
      resources:
        requests:
          cpu: "100"
          memory: "128Gi"