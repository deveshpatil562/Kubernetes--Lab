apiVersion: v1
kind: Pod
metadata:
  name: readiness-demo
  labels:
    appp: readiness-demo
spec:
  containers:
    - name: nginx
      image: nginx
      readinessProbe:
        httpGet:
          path: /
          port: 80
        initialDelaySeconds: 5
        periodSeconds: 5
          